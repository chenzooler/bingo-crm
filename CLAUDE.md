# BINGO CRM — מדריך הפרויקט (קרא ראשון)

> ## 🚀 סשן חדש / מחשב חדש? — התחל כאן
> **התקנה (פעם אחת):** `npm install` → `npx prisma generate` → `npm run dev -- -p 7800`
> → לגלוש ל-`http://localhost:7800/leads/1`. מסד הנתונים (SQLite עם נתוני הדמו) כבר
> בתוך הפרויקט ב-`prisma/dev.db` — אין מה להגדיר.
> **⚠️ יש עבודה לא-מקומטת בעץ** (כל העבודה של 06/07/26 — מתועדת בסטטוס למטה).
> אל תעשה push בלי אישור מחן — push ל-main פורס אוטומטית לפרודקשן ב-Coolify!
> **איפה עצרנו + מה הלאה:** סקשן "🧭 סטטוס נוכחי" למטה. מסמכי החובה:
> `docs/yoatsim-full-spec.md` (האפיון) · `docs/master-plan.md` (תוכנית-האב) ·
> `docs/retzef-spec.md` (רֶצֶף). **ההחלטה האסטרטגית האחרונה של חן: קודם שכפול 1:1
> של Yoatsim (בנוי!), אחר-כך מוסיפים פיצ'רים.**

> **מטרה:** לבנות את מערכת ה-CRM המתקדמת בעולם עבור **בינגו — מימון בול בשבילך**,
> חברת תיווך אשראי ישראלית. המערכת מחליפה בהדרגה את Yoatsim (app.yoatsim.co.il).
> המשתמש: **חן צולר** (מנכ"ל). עברית RTL. מעדיף עברית בתקשורת.

---

## ⚡ הקשר מהיר לסשן חדש
- זה פרויקט **Next.js 16 + React 19 + Tailwind v4 + TypeScript + Prisma (SQLite)**.
- מריצים מ-`/Users/zooler/Desktop/בינגו crm`. `npm run dev` (פורט 7800 בד"כ).
- פרוס אוטומטית ב-**Coolify** על `crm.bingoisrael.co.il` בכל `git push` ל-main.
- **מודל:** Fable 5. אם מנגנון הבטיחות מסמן ומחליף ל-Opus — זה צד-שרת, לא באג בקוד.
  כדי להימנע: בקוד השתמש במונחים ניטרליים (screening/indicator) במקום
  "אשראי/BDI/סמיילי" בשמות משתנים כשאפשר. `switchModelsOnFlag: false` כבר מוגדר.

## 📚 מסמכי אפיון — קרא לפני שבונים כרטיס/תהליכים
- **`docs/yoatsim-full-spec.md`** ⭐ — האפיון המלא של Yoatsim (כרטיס לקוח מלא,
  15 תהליכים + כל הסטטוסים, 21 אוטומציות, 42 תבניות הודעה, 27 משתמשים + 9 הרשאות).
  **זה מקור האמת. כל בנייה מבוססת עליו.**
- **`docs/yoatsim-lead-card.md`** — תיעוד חי של כרטיס הלקוח האמיתי (שני הרמזורים,
  שאלון האשראי המדויק).
- **`docs/yoatsim-audit.md`** — מודל הנתונים הפנימי (קודי שדות a88/n4/s7, קודי
  אופציות, 14 בנקים), 10 תהליכים + ספירות (~160K לידים).
- **`docs/bingo-operation-mastery.html`** — ניתוח מלא של מערכת הבוט (5 גופי מימון,
  כללי עסק, אוטומציות, בעיות).

## 🏢 איך העסק עובד (התמצית)
הנציג לוחץ "תותח שיחות" → קופץ לקוח → שאלון מיון (בדיקת רמזור/סמיילי) →
לפי התוצאה מנתב ל-**כל מטרה** (תקין) או **רכב** (נפסל+יש רכב) → מחתים על הסכם
התקשרות → חוזר אחרי שעה → בדיקות זכאות ב-5 גופי מימון (ירושלים/פניקס/ישראכרט/
כאל/מקס) או בקשת מסמכים (רכב) → משקף תוצאות → ממתין להלוואה → גובה תשלום.

**כללי ניתוב:** סמיילי צהוב/אדום (אוטומטי או ידני) · אין כרטיס אשראי · מסגרת עד
5,000₪ · מטרה=רכב → כולם מנתבים למסלול רכב (אם יש רכב; אחרת יציאה).

## 🔴 הבעיה המרכזית שאנחנו פותרים
ב-Yoatsim ליד יכול להשתייך ל**מספר תהליכים בו-זמנית** (למשל "BDI שלילי" + "ליד
חדש רכב"). זה יוצר בלגן אדיר. **הפתרון ב-Bingo: ליד אחד = מסע אחד.** "יש רכב" =
תכונה שהמערכת זוכרת ומקפיצה אוטומטית כשצריך, לא תהליך נוסף.

---

## 🎨 מערכת העיצוב (Brand-True v3)
מבוסס על bingoisrael.co.il האמיתי. הכללים ב-`app/globals.css`:
- **פונט:** `Polin` (רשמי, ב-`public/fonts/`), נופל ל-Heebo.
- **צבעים:** `#292929` (טקסט) · `#50FF0A` (ירוק אקסנט) · `#1F81D6` (כחול) ·
  `#FAFAF9` (רקע) · tints: `#DAFFCB` `#C0CFE2` `#FFBC7D`.
- **צורה:** גלולות 50px (`b-pill`), כרטיסים 20px (`b-card`).
- **קלאסים מוכנים:** `b-card` · `b-pill-{dark,green,ghost}` · `b-chip-{green,blue,orange,red,gray,dark}`
  · `b-icon-{green,blue,orange,red,purple,gray}` (עיגולים מגוונים) · `b-input`
  · `b-progress` · `b-segment` · `b-eyebrow` · `b-ball` (כדור הלוגו).
- שכבת תאימות: קלאסים ישנים (`btn-vibrant`, `card-apple`, `icon-3d-*`) ממופים
  אוטומטית לסגנון המותג — לא לשבור.

## 🗂️ מבנה
```
app/(app)/          — כל המסכים (dashboard, leads, dialer, briefing, wallboard,
                       inbox, calls, calendar, admin, admin/import, tasks...)
app/(app)/leads/[id]/page.tsx  → LeadCardV3 (כרטיס הלקוח המונחה)
app/sign/[leadId]/  — דף חתימה ציבורי ללקוח
app/api/            — leads, import, providers, lenders, stats
components/lead/LeadCardV3.tsx  — כרטיס הלקוח (מנוע המסע)
components/layout/  — Header, NavRail, Sidebar, TasksPanel
lib/journey.ts      — מנוע המסע (state machine) — משקף את כרטיס Yoatsim
lib/db.ts + prisma/ — Prisma + SQLite; seed בו 26 משתמשים + 12 גופים
lib/import/         — מנוע ייבוא Excel/CSV מ-Yoatsim (זיהוי עמודות עברית)
```

## 🧭 סטטוס נוכחי / איפה ממשיכים
- ✅ עיצוב מותג, NavRail, Dashboard, ייבוא DB, דף חתימה, wallboard, briefing — בנויים.
- ✅ **מיילסטון 1 הושלם (06/07/26): כרטיס הלקוח command-center** —
  `components/lead/journey/` (JourneyCard + useJourney + 13 סקשנים + StageView +
  JourneyMap + VehiclePivot). מקלדת-first (Enter/1-9/Esc/?), מעברי framer-motion,
  רגע רמזור כפול, פיבוט רכב מסך-מלא, קונפטי. הכרטיס הישן LeadCardV3 נמחק
  (וגם LeadJourney/LeadCard/WizardShell/LiveAgentTicker המתים).
- ✅ **פרסיסטנטיות בשרת**: `Lead.journeyJson/journeyUpdatedAt/journeyVersion` (מיגרציה
  journey_persistence) · `lib/journey-db.ts` (מיפוי עברית↔DB דו-כיווני + מילוי-מראש
  מלידים מיובאים) · `PUT /api/leads/[id]/journey` (נעילה אופטימית 409, Activity אוטומטי,
  מראה stage) · `POST /api/leads/[id]/activities`. אין יותר localStorage.
- ✅ `deriveStage()` ממפה מסע→stage לפי אוצר lifecycle.ts (NEW→CONTACT→SCREENING→
  CONTRACT→BDI→AUCTION→DECISION→DOCS→DISBURSEMENT→PAID|EXIT).
- ✅ אומת: 40 בדיקות API E2E (שני המסלולים עד PAID), tsc נקי, build פרודקשן נקי.
- ✅ **פידבק חן (06/07/26)**: `LeadDataSheet` (תיק לקוח — מגירת כל הנתונים, כפתור בכותרת) ·
  באנר הקשר בכותרת (`journeyContext()` — ליד חדש/חוזר/ביקש חזרה/חתום+מסלול) ·
  **סיבות מסלול רכב** (`vehicleReason`: נפסל בסינון/סורב בכל מטרה/סכום לא הספיק/משולב) ·
  **מסלול משולב** (`comboVehicle` — כל מטרה + רכב במקביל, בדיקות→תוצאות→מסמכים→תשלום).
  נקודות כניסה: הפיבוט (אוטומטי) + כפתורים בסקשן התוצאות.
- ✅ **רֶצֶף (06/07/26)** — הפרדיגמה החדשה אחרי פידבק "מסורבל" של חן. עוצב ב-workflow
  רב-סוכני (4 חזונות → 3 שופטים → סינתזה): **`docs/retzef-spec.md`** ⭐ (קרא לפני שנוגעים).
  העיקרון: קלף אחד · תסריט חי · אפס אבודים. מה נבנה (Stage 1, שכבת תצוגה על המנוע הקיים):
  - `nextActionFor()` ב-lib/journey.ts — לכל ליד פעיל בדיוק צעד-הבא אחד (נגזר-בקריאה).
  - `lib/script.ts` — ביטים של התסריט (מחרוזות תצוגה בלבד, אפס לוגיקה).
  - `SpeakableStage` — פני-השיחה: משפט ענק שמקריאים + שדה מתחת, Enter שואב את הערך
    לזרם העבר (זהב), **הפיבוט = מורף של המשפט** (בלי מודאל). ברירת המחדל; `?view=form` = הטופס הישן.
  - `GhostFooter` — חוזה הצעד-הבא חי בתחתית הכרטיס.
  - `OutcomePanel` — Esc = חותמת סיום שיחה, מיפוי 1-9 קבוע, Enter = ההצעה של המנוע.
  - `/desk` ("רֶצֶף" ב-NavRail) — הקלף המוגש + רצועת היום + מונה "לידים בלי צעד הבא: 0".
- ✅ **שכפול Yoatsim 1:1 (06/07/26) — ההחלטה האסטרטגית של חן: קודם שכפול מדויק, אחר כך שיפורים.**
  **הכרטיס הקלאסי הוא ברירת המחדל ב-`/leads/[id]`** (רֶצֶף ב-`?view=retzef`, הטופס ב-`?view=form`).
  - `lib/yoatsim/processes.ts` — 15 התהליכים + 179 סטטוסים, מילה במילה מהאפיון.
  - `lib/yoatsim/card-schema.ts` — 15 סקשני הכרטיס, כל שדה/dropdown כמו במקור.
  - `lib/yoatsim/values.ts` — ערכים ב-`Lead.extraJson` + מראה לעמודות קנוניות.
  - `components/classic/` — ClassicLeadCard (3 עמודות: פרטי כרטיס · משימות ופעילויות עם
    פילטרי אייקונים · אנשי קשר+תהליכים+אחראי), FieldRenderer, LendersSection (6 גופים +
    סה"כ אוטומטי), SmileyBanner, ProcessesRail.
  - **מודל `LeadProcess`** (מיגרציה yoatsim_clone) — ריבוי תהליכים לליד כמו במקור,
    API ב-`/api/leads/[id]/processes`, כל שינוי סטטוס נרשם ל-Activity.
  - ⚠️ אחרי מיגרציה חדשה חובה להפעיל מחדש את שרת ה-dev (client ישן בזיכרון → 500).
- ✅ **השלמת השכפול — מסך ראשי + כל ההגדרות (06/07/26, המחשב החדש; נתיב:
  `/Users/zooler-krannot/Desktop/בינגו crm 2026`)**. נבנה בפעם אחת לפי בקשת חן
  ("הכל כולל הכל עם ההגדרות"):
  - **מודלים חדשים** (מיגרציה yoatsim_settings_clone): `Task` (משימות + "מתפרצת"),
    `Automation` (21), `MessageTemplate` (32), `User.permissionRole` (9 רמות).
    seed.ts זורע הכל מהאפיון (idempotent, לא דורס עריכות UI).
  - **שכבת נתונים**: `lib/yoatsim/automations.ts` (21 האוטומציות, שמות מהאפיון;
    תנאים/פעולות משוחזרים — ניתנים לעריכה) · `lib/yoatsim/templates.ts` (32 תבניות,
    גופים משוחזרים) · `lib/yoatsim/permissions.ts` (9 פרופילים + מטריצה מלאה).
  - **מסך ראשי `/leads`** — שכפול Main.aspx: 4 כפתורי פעולה (משימות באיחור/נצפו
    לאחרונה [localStorage]/ליד חדש בטיפולי/תצוגת דוח) · חיפוש + לפי משתמש/תהליך ·
    עץ 15 התהליכים עם ספירות חיות + הרחבת סטטוסים · טאבים כרטיסים/שכפול/בדיקה/אנשי
    קשר · טבלה (7 עמודות, פילטר פר-עמודה, 100 בעמוד, צ'יפים מרובי-תהליכים) ·
    `components/yoatsim-main/`. המסך הישן ב-`?view=bingo`.
  - **API**: `/api/tasks` (+[id]) · `/api/automations` (+[id]) · `/api/templates` (+[id]) ·
    `/api/users` (+[id]) · `lib/current-user.ts` (חן קשיח — התפר ל-auth אמיתי).
  - **TasksPanel** מחובר ל-DB (נכנסות/יוצאות/עתידי, סמן-כבוצעה אופטימי).
  - **הגדרות**: `/settings/users` (DB, בחירת רמת הרשאה) · `/settings/permissions`
    (מטריצת 9 הרמות + טבלת השוואה) · `/settings/processes` (15 תהליכים, ספירות חיות) ·
    `/settings/fields` (הגדרת שדות מ-card-schema) · `/settings/templates` (DB, עריכה
    מלאה + ולידציית WATI JSON) · `/automations` (DB, מבנה בסיס→תנאים→פעולות, עורך
    מובנה, מתג חי) · hub + layout עודכנו לפי §2.
  - אומת: tsc נקי · build פרודקשן נקי · אימות ויזואלי מלא של כל מסך ב-preview ·
    סינון תהליך/סטטוס מאומת · ליד 4 מדגים ריבוי תהליכים (BDI שלילי + שיעבוד חדש).
- ✅ **פריטת מלאה ליועצים (06/07/26, גל שני)** — "תמשיך עד הסוף" של חן:
  - **מנוע אוטומציות רץ בפועל**: `lib/yoatsim/engine.ts` — מופעל בכל שמירת כרטיס
    (PATCH `/api/leads/[id]`), **טריגר-קצה** (נורה רק כשהתנאים הופכים למתקיימים —
    לא בכל autosave), מיפוי label→key מ-card-schema + כינויים, ערכים מיוחדים
    (מולא/הגיע/היו בעיות/רמזורים), add-process משודרג ל-upsert + Activity,
    send-template נרשם כסימולציה. אומת E2E דרך ה-UI האמיתי (יש רכב לא→כן ⇒
    "רכב-לא משועבד" הוסיפה מחלקת רכב/שיעבוד חדש + Activity).
  - **תת-סוגי כרטיס אמיתיים** (מיגרציה yoatsim_full_parity): `Lead.cardKind`
    (card/duplicate/test) + `parentLeadId` + `archived`. כפתורי שכפול/כרטיס
    בדיקה/ארכיון בכותרת הכרטיס עובדים (`/api/leads/[id]/clone`), טאבי המסך
    הראשי מסננים באמת, טוגל ארכיון. "ליד חדש" ב-Header יוצר ליד אמיתי
    (מחלקת החתמות/ליד חדש) ונכנס אליו.
  - **`/tasks`** — עמוד מלא: פילטרי המקור (✓/עתידי/יוצאות/נכנסות/WhatsApp/🔕),
    +משימה/+מתפרצת עם בורר ליד חי, 25 בעמוד. **`/reports`** — תצוגת דוח אמיתית
    (לפי תהליך+סטטוסים/אחראי/מקור/משפך 6 חודשים/משימות, ?user= ?process=).
    **`/search`** — חיפוש מתקדם אמיתי (כל השדות, טווחים, סוג כרטיס, ארכיון).
  - **הגדרות הושלמו**: קבלת לידים/API (endpoint אמיתי `POST /api/intake` עם
    x-api-key מ-AppSetting) · דפי נחיתה · בוט ווטסאפ · מודולים · תבניות
    פעולות/משימות/פגישות/כספים · טפסים (מודל `SentForm`, `/api/forms`).
    מודל `AppSetting` גנרי + דיפולטים ב-`lib/yoatsim/app-defaults.ts`.
  - **הכרטיס**: סקשן "טפסים וקבצים" שולח/מסמן-נחתם באמת (SentForm + Activity);
    "מקור הליד" מציג נתוני מקור אמיתיים.
  - תוקן: `GET /api/leads?q=` עברית כבר לא תופס הכל (digits-only לטלפון/ת.ז).
  - אומת: tsc 0 שגיאות · build פרודקשן נקי · אימות ויזואלי לכל המסכים ·
    E2E למנוע/שכפול/ארכיון/intake/טפסים עם שחזור נתוני הדמו לבסיס.
- ✅ **מצב שכפול-יועצים — הסתרת כל תוספות בינגו (06/07/26)**. פידבק חן: "שזה ממש
  יראה בדיוק אותו דבר, אותו מבנה, אותו סדר — תסתיר את כל התוספות". מה נעשה:
  - **`/` → `/leads`** (המסך הראשי = הבית, כמו Main.aspx). גם הלוגו ב-Header.
  - **NavRail**: רק "ראשי" + "הגדרות". כל השאר (בוקר/רֶצֶף/הבית/תותח/הודעות/שיחות/
    יומן/משימות/דוחות/מסך חי/סנכרון/מנהל) הועבר ל-`NAV_HIDDEN` באותו קובץ —
    להחזרה מעבירים חזרה ל-NAV. משימות מגיעים מהפאנל ("הצג הכל"), דוחות מכפתור
    "תצוגת דוח" — כמו במקור. MobileBottomNav צומצם באותה רוח.
  - **Header**: SearchTrigger + CommandPalette (⌘K) מוסתרים בקומנטים; עוזר ה-AI
    (AIAssistant/Launcher) הוסר מ-`app/(app)/layout.tsx` (קומנט מסביר החזרה).
  - **הגדרות**: התפריט (`settings/layout.tsx`) = בדיוק רשימת §2 בסדר המקורי
    (14 סקשנים); התוספות (SLA/מספרים/תמחור/צוותים/lifecycle/גופי מימון/אבטחה...)
    ב-`NAV_HIDDEN`. מרכזיית `/settings` = אריחי הסקשנים בלבד (טופס "הגדרות
    כלליות" של בינגו הוסר — git שומר).
  - כל הדפים המוסתרים חיים ב-URL ישיר — שום דבר לא נמחק.
  - אומת: tsc 0 · build נקי · אימות ויזואלי דסקטופ+מובייל.
- ✅ **הקוקפיט + שפת הפרימיום v2 (07/07/26)** — אחרי קונספט שאושר ע"י חן
  (artifact: claude.ai/code/artifact/3bc6d818-1101-43b9-9199-b8e391d35db4, מבוסס
  מחקר פינטק/מוקדים). **"סמיילי" נקרא מעכשיו "רמזור" בכל המערכת** (keys נשארו
  smileyAuto/Manual; seed מריץ rename ל-DB רץ; המנוע מכיר את שני השמות + ערכי
  הניסוח החדש דרך NO_PROBLEM_VALUES).
  - **שפת חומרים** (globals.css "Premium v2"): b-glass/b-obsidian/b-tint-*/b-lift/
    b-spring-in + b-pop/b-scan/b-pulse-glow · רקע שמנת+גלואים (b-app-ambient) ·
    `components/ui/Ramzor.tsx` (רמזור תלת-ממדי, sm/md/lg, scanning/onSelect) ·
    GlassIcon · עמוד QA ב-`/design-lab` (מוסתר).
  - **הקוקפיט** (`components/lead/cockpit/`) — ברירת המחדל ב-`/leads/[id]`
    (הקלאסי ב-`?view=classic`, עם טינטים גם הוא): CockpitCard (מצב שיחה/תיק מלא,
    נגזר מ-SentForm חתום/stage; טוגל ידני) · ScreeningGates (5 שערים בהתניות,
    נעולים עד מענה, מקלדת 1/2/Enter, ניסוח חדש שאישר חן, "תוצאות שאלון" נמחק) ·
    RamzorModal (נוסח הקראה, הסכמה נרשמת כ-Activity, סריקה, בחירה ידנית אם אין
    smileyAuto, ניתוב: ירוק←כתובת+הסכם / כתום←רכב / אדום←יציאה) · WaitSignature
    (נשלח/סמן-כנחתם ← תיק מלא + קונפטי) · ClassicSections.tsx משותף עם מפת טינטים.
  - אומת ידנית מקצה לקצה דרך ה-UI (שערים←רמזור ירוק←הסכם←חתימה←תיק מלא; מסלול
    מסומן←רכב ע"י הסוכן) · tsc 0 · build נקי · דמו-DB שוחזר לבסיס.
- ⏭️ **הבא בתור**: פידבק חן על הקוקפיט בלייב · dashboard על נתונים אמיתיים ·
  שליחת תבניות אמיתית (ספק SMS/WATI) · אינטגרציית בדיקת רמזור אמיתית (כיום
  הנציג בוחר צבע ידנית כשאין smileyAuto) · ייבוא/סנכרון 160K לידים מיועצים
  (מחכה לקובץ ייצוא מחן) · החזרת מסכי בינגו המוסתרים (NAV_HIDDEN) בהדרגה.
  בהמתנה: רֶצֶף Stage 2-3 + תוכנית-האב (master-plan.md).

## 🛠️ פקודות
```bash
npm run dev            # שרת פיתוח
npm run build          # בילד (prisma generate + next build)
npm run db:migrate     # מיגרציית Prisma
npm run db:seed        # זריעת משתמשים+גופים
git push               # → Coolify בונה ופורס אוטומטית
```

## ✍️ מוסכמות
- קוד באנגלית, טקסט UI + קומנטים עסקיים בעברית.
- Commits באנגלית, מסתיימים ב-`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- אמת ויזואלית עם preview לפני push. אל תיגע בקוד הבוט/דפדפן (תיקיית "בינגו בדיקות").
